# [{{ severity }}] Reflected Cross-Site Scripting (XSS) on {{ affected_url }}

## Vulnerability Details
- **Vulnerability Class:** Reflected Cross-Site Scripting (XSS)
- **Affected URL:** {{ affected_url }}
- **Affected Parameter:** {{ affected_param }}
- **Severity:** {{ severity }}

## Summary
The endpoint `{{ affected_url }}` is vulnerable to reflected Cross-Site Scripting (XSS) via the `{{ affected_param }}` parameter. The application fails to properly sanitize or encode user-supplied input before reflecting it back in the response, allowing an attacker to execute arbitrary JavaScript in the context of the victim's session.

## Reproduction Steps
1. Navigate to the following URL:
   ```
   {{ poc_url }}
   ```
2. Observe the JavaScript execution (e.g., an alert box displaying `{{ alert_text }}`).

## Proof of Concept (PoC)
The following payload was used to trigger the vulnerability:
```
{{ payload }}
```

**Request Chain:**
```http
{{ request_dump }}
```

**Response Snippet:**
```html
{{ response_snippet }}
```

## Impact
An attacker could use this vulnerability to steal session cookies, perform unauthorized actions on behalf of the user, or deliver malicious payloads to victims.

## Remediation Suggestion
Implement robust input validation and context-aware output encoding. Use a security-focused library or framework features to automatically encode data based on where it is being placed in the HTML (e.g., HTML body, attribute, script tag).

---
*Report generated by VulnForge*
